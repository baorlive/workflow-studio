/**
 * UI Constants
 * Centralized magic numbers for consistent layout and behavior.
 */

// WorkflowCard
export const WORKFLOW_CARD_VISIBLE_ICONS = 2;

// Node dimensions (shared between canvas and services)
export const NODE_WIDTH = 256;
export const NODE_HEIGHT = 80;

// Canvas
export const CANVAS_PADDING = 50;

// Grid layout
export const GRID_COLS_DESKTOP = 4;

// Pagination
export const ITEMS_PER_PAGE = 9;
